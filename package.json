{
  "name": "agentcommit-monorepo",
  "version": "0.1.0-alpha.1",
  "private": true,
  "description": "Transactional safety for AI agents. Preview, commit, and safely roll back agent changes.",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "test": "vitest run",
    "smoke": "node packages/cli/dist/index.js --help"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "eslint": "^10.9.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.69.0",
    "vitest": "^5.0.0"
  }
}
